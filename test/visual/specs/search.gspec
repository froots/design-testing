@objects
    search      .search
        label   .search__label
        input   .search__input
        button  .search__button

= Main section =
    @on *
        search:
            width 100% of viewport/width

        search.label:
            inside search 10px top, 10px left, 10px right

    @on narrow_screen
        search:
            % image file ../reference/search-small.png, file ../reference/search-medium.png, file ../reference/search-large.png, error 5%

        search.label:
            aligned vertically all search.button
            aligned vertically all search.input

        search.input:
            inside search 10px left, 10px right
            aligned vertically all search.button
            aligned vertically all search.label
            below search.label 8px

        search.button:
            inside search 10px left, 10px right, 10px bottom
            aligned vertically all search.input
            aligned vertically all search.label
            below search.input 8px

    @on wide_screen
        search.label:
            aligned vertically left search.input
            aligned vertically right search.button

        search.input:
            inside search 10px left, 10px bottom
            aligned horizontally all search.button
            below search.label 8px

        search.button:
            inside search 10px right, 10px bottom
            right-of search.input 5 to 10 px
